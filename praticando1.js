const numero1 = 50;
const numero2 = 43;
const numero3 = 12;

// utilizando array com 3 elementos
const numeros = [50, 43, 12];

const somaNumeros = (numeros[0] + numeros[1] + numeros[2])
const multiplicaNumeros = (numeros[0] * numeros[1] * numeros[2])
const mediaNumeros = (numeros[0] + numeros[1] + numeros[2]) / numeros.length; // .length especifica o tamanho de um array

console.log(somaNumeros)
console.log(multiplicaNumeros)
console.log(mediaNumeros)

const arrayDeNumeros = [31, 01, 16];
const arrayDeStrings = ["morango", "alura", "Leandro"];

console.log(arrayDeNumeros);
console.log(arrayDeStrings);
console.log(arrayDeStrings.length);